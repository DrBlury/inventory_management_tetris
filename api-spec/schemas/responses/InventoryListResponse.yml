---
type: object
properties:
    inventories:
        type: array
        items:
            $ref: '../types/Inventory.yml'
    pagination:
        $ref: '../types/Pagination.yml'

required:
    - inventories
    - pagination
