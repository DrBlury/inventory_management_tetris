---
type: object
description: This object holds the user list response data. It includes the list of users and pagination data.
properties:
  users:
    $ref: '../types/UserList.yml'
  pagination:
    $ref: '../types/Pagination.yml'

example:
  $ref: '../../examples/_index.yml#/UserListResponseExample'

required:
  - users
  - pagination
