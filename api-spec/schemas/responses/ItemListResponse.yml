---
type: object
properties:
    items:
        type: array
        items:
            $ref: '../types/Item.yml'
    pagination:
        $ref: '../types/Pagination.yml'

required:
    - items
    - pagination
