---
type: object
description: This object holds the item list response data. It includes the list of items and pagination data.
properties:
  items:
    $ref: '../types/ItemList.yml'
  pagination:
    $ref: '../types/Pagination.yml'

example:
  $ref: '../../examples/_index.yml#/ItemListResponseExample'

required:
  - items
  - pagination
