---
type: object
properties:
    name:
        type: string
    max_weight:
        type: number
    user:
        $ref: '../types/User.yml'
    volume:
        $ref: '../types/Volume.yml'

required:
    - name
    - max_weight
    - user
    - volume
