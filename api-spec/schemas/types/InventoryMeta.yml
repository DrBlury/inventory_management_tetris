type: object
description: This object holds the inventory meta data (restrictions, owner, etc.)
properties:
  id:
    type: integer
  name:
    type: string
  userId:
    type: integer
  max_weight:
    type: integer
  volume:
    $ref: './Volume.yml'

required:
  - id
  - name
  - userId
  - max_weight
  - volume
